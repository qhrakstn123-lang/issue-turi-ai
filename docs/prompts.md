# 프롬프트 설계

MVP 1은 fake agent를 사용하므로 실제 프롬프트를 호출하지 않는다.

실제 LLM 연결 단계에서는 `prompts/` 아래 파일을 로드하고, 모델 응답을 JSON schema로 검증한다.

필수 원칙:

- 참고 채널의 대사, 자막, 썸네일, 고유 표현을 복제하지 않는다.
- 루머는 사실처럼 단정하지 않는다.
- 장면별 필수 필드가 빠지면 실패로 처리한다.
- 생성 결과가 너무 유사하거나 위험하면 `review_required`로 표시한다.
